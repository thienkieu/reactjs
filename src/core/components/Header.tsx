import * as React from "react";
import * as coreCss from '../core.css';

export const Header = () => {
    return (
        <header className={coreCss.header} >
            <div className={coreCss.sectionContent}>this is header</div>
        </header>
    );
}