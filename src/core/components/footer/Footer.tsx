import * as React from "react";
import * as coreCss from '../../../core/core.scss';

export const Footer = () => {
    return (
        <footer className={coreCss.footer} >
            <div className={coreCss.sectionContent}>This is footer</div>
        </footer>
    );
}